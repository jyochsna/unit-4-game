$(document).ready(function () {
    var ranMin = 19;
    var ranMax = 121;
    //for random number array
    var num = [];
    for (var n = ranMin; n < ranMax; n++) {
        num.push(n);
    }
    var ranNumber;//number to match
    //for randnom number array for crystals
    var crystal1;
    var crystal2;
    var crystal3;
    var crystal4;

    var totalScore = 0;
    var wins = 0;
    var losses = 0;
      //functions for randomnuber generated by computer
    function pickRandomNumber(min, max) {
        var x = Math.floor(Math.random() * (max - min + 1) + min);
        ranNumber = x;
        $("#number2guess").text("Match this Number: " + ranNumber);
        console.log(ranNumber);
    }
    
    //function for crystals Value arr

    var startGame = function(){
        crystal1 = Math.floor((Math.random()*12)+1);
        crystal2 = Math.floor((Math.random()*12)+1);
        crystal3 = Math.floor((Math.random()*12)+1);
        crystal4 = Math.floor((Math.random()*12)+1);
        totalScore = 0;
       $("#totalscore").text("Your Total Score is: " + totalScore);
    };
    startGame();

// click function for crystals
$("#btn-1").click(function(){
    totalScore = totalScore + crystal1;
    $("#totalscore").text("Your Total Score is: " + totalScore);
})
$("#btn-2").click(function(){
    totalScore = totalScore + crystal2;
    $("#totalscore").text("Your Total Score is: " + totalScore);
})
$("#btn-3").click(function(){
    totalScore = totalScore + crystal3;
    $("#totalscore").text("Your Total Score is: " + totalScore);
})
$("#btn-4").click(function(){
    totalScore = totalScore + crystal4;
    $("#totalscore").text("Your Total Score is: " + totalScore);
})
// finctions for wins and losses
$("button").click(function(){

if(totalScore == ranNumber){
    alert("You Win");
    wins++;
    $("#wins").text("Wins: " + wins);
    startGame();
   
}
if(totalScore > ranNumber){
    alert("Sorry, You Lost  "  + "\n Your total score is: " + totalScore);
    losses++;
    $("#losses").text("Losses: " + losses);
    startGame();
   

}
})
    pickRandomNumber(ranMin , ranMax);


});